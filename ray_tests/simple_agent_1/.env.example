# Teal Agents Configuration for FileAssistant with Merck API
# This .env file is local to the test directory for self-contained testing

# Agent Configuration File (required)
# Path to the agent's config.yaml file (relative to this directory)
TA_SERVICE_CONFIG=config.yaml

# Plugin Module (required)
# Path to the Python file containing custom plugins (relative to this directory)
TA_PLUGIN_MODULE=file_plugin.py

# Custom Chat Completion Factory (required for Merck API)
# Path to the custom factory module (relative to this directory)
TA_CUSTOM_CHAT_COMPLETION_FACTORY_MODULE=merck_chat_completion_factory.py
# Class name of the custom factory
TA_CUSTOM_CHAT_COMPLETION_FACTORY_CLASS_NAME=MerckChatCompletionFactory

# Merck API Configuration (required)
# X-Merck-APIKey for authentication with Merck's internal GPT API
MERCK_API_KEY=your-x-merck-apikey-here
# Base URL for Merck GPT API (default: https://iapi-test.merck.com/gpt/v2)
MERCK_API_ROOT=https://iapi-test.merck.com/gpt/v2

# Legacy API Key (kept for backwards compatibility, not used with custom factory)
TA_API_KEY=not-used-with-custom-factory

# Authorization (required for the platform)
# Using the dummy authorizer for testing (no real auth needed)
# Note: Use absolute path for TA_AUTHORIZER_MODULE
TA_AUTHORIZER_MODULE=/path/to/teal-agents/src/sk-agents/src/sk_agents/authorization/dummy_authorizer.py
TA_AUTHORIZER_CLASS=DummyAuthorizer

# Optional: Telemetry and Logging
# TA_TELEMETRY_ENABLED=false
# TA_LOG_LEVEL=INFO

# GitHub OAuth2 Configuration for MCP Testing
# Create a GitHub OAuth App at: https://github.com/settings/developers
# Authorization callback URL: http://localhost:9001/oauth/github/callback
#
# Note: CLIENT_ID will be prompted for interactively when starting the OAuth server.
# This simulates production architecture where the platform stores secrets securely
# but users provide their OAuth app client IDs at runtime.
GITHUB_OAUTH_CLIENT_SECRET=your_github_oauth_client_secret_here
GITHUB_OAUTH_REDIRECT_URI=http://localhost:9001/oauth/github/callback

# Redis AuthStorage Configuration (REQUIRED for OAuth testing)
# OAuth handler (port 9001) and agent (port 8000) are separate processes
# They MUST share tokens via Redis, not in-memory storage
# Start Redis before testing: `redis-server` or `docker run -d -p 6379:6379 redis`
TA_AUTH_STORAGE_MANAGER_MODULE=/Users/suray2/local_repos/internal_AI/teal-agents/src/sk-agents/src/sk_agents/auth_storage/custom/example_redis_auth_storage.py
TA_AUTH_STORAGE_MANAGER_CLASS=RedisSecureAuthStorageManager
TA_REDIS_HOST=localhost
TA_REDIS_PORT=6379
TA_REDIS_DB=0
TA_REDIS_TTL=3600
